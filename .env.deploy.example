# Deploy script configuration
# Copy to .env in project root, or export before running ./deploy.sh

# Nginx port (default: 80 for production)
APP_PORT=80

# Full rebuild without cache (slower)
# NO_CACHE=1

# Rebuild nginx after nginx.conf changes
# REBUILD_NGINX=1

# Skip database migrations
# SKIP_MIGRATE=1

# Validate only, don't deploy
# DRY_RUN=1
